//Просто и эффективно, когда вам нужно активировать аппаратное ускорение для некоторой анимации, чтобы все было быстро, гладко и без мерцания.
@mixin hardware($backface: true, $perspective: 1000) {
  @if $backface {
    backface-visibility: hidden;
  }
  perspective: $perspective;
}
