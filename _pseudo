/Коли ви використовуєте ::before і ::after, ви завжди потребуєте цих трьох рядків коду//
@mixin pseudo($display: block, $pos: absolute, $content: "") {
  content: $content;
  display: $display;
  position: $pos;
}
